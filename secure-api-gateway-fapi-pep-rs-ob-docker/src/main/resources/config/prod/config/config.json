{
  "properties": {
    "routesServiceScanInterval": "disabled"
  },
  "handler": {
    "name": "_router",
    "type": "Router",
    "config": {
      "scanInterval": "disabled",
      "directory": "${openig.configDirectory}/routes"
    }
  }
}
